{
  "name": "@glimmer-cradle/template-basic",
  "version": "0.2.1",
  "private": true,
  "description": "First-party Glimmer Cradle extension template.",
  "license": "GPL-3.0-only",
  "packageManager": "pnpm@11.13.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node scripts/clean-dist.mjs && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('template-basic smoke ok')\"",
    "release:prepare": "pnpm typecheck && pnpm test && pnpm build && node scripts/build-release.mjs"
  },
  "peerDependencies": {
    "@glimmer-cradle/extension-sdk": "0.2.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^6.0.2",
    "yaml": "^2.8.1",
    "zod": "^3.23.0"
  }
}
